package com.gabriel.bate_ponto.model;


// enum serve para representar valores que nao mudam, como a role, os cargos e tambem os valores do ponto
public enum TipoPonto {
    INICIO,
    INTERVALO,
    FIM_INTERVALO,
    SAIDA
}
